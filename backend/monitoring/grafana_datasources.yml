# Grafana Data Sources Configuration
# Import this into Grafana via API or UI

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://localhost:9090
    isDefault: true
    editable: true











